---
import WidgetWrapper from '~/components/ui/WidgetWrapper.astro';
import Headline from '~/components/ui/Headline.astro';
import { Icon } from 'astro-icon/components';

interface Service {
  title: string;
  description: string;
  icon: string;
  features: string[];
  price?: string;
  popular?: boolean;
}

interface Props {
  title?: string;
  subtitle?: string;
  tagline?: string;
  services?: Service[];
  id?: string;
  isDark?: boolean;
  classes?: Record<string, string>;
  bg?: any;
}

const {
  title = 'Our Digital Services',
  subtitle = 'Comprehensive solutions to grow your business online',
  tagline = 'Services',
  services = [
    {
      title: 'Website Development',
      description: 'Modern, responsive websites built with cutting-edge technology. Perfect for businesses looking to establish a strong online presence.',
      icon: 'tabler:world',
      features: ['Custom Design', 'Mobile-First', 'SEO Optimized', 'Fast Loading', 'CMS Integration', '24/7 Support'],
      price: 'From Rs. 25,000'
    },
    {
      title: 'Mobile App Development',
      description: 'Native and cross-platform mobile applications for iOS and Android. We build apps that users love and businesses rely on.',
      icon: 'tabler:device-mobile',
      features: ['iOS & Android', 'Cross-Platform', 'UI/UX Design', 'App Store Optimization', 'Maintenance', 'Updates'],
      price: 'From Rs. 75,000'
    },
    {
      title: 'SEO & Digital Marketing',
      description: 'Drive traffic and increase conversions with our comprehensive digital marketing strategies tailored for the Nepali market.',
      icon: 'tabler:chart-line',
      features: ['Keyword Research', 'Content Strategy', 'Link Building', 'Analytics', 'Monthly Reports', 'ROI Tracking'],
      price: 'From Rs. 15,000/month'
    },
    {
      title: 'E-commerce Solutions',
      description: 'Complete online store development with payment gateways, inventory management, and marketing tools to boost your sales.',
      icon: 'tabler:shopping-cart',
      features: ['Payment Integration', 'Inventory Management', 'Order Processing', 'Security', 'Analytics', 'Marketing Tools'],
      price: 'From Rs. 45,000'
    }
  ],
  id,
  isDark = false,
  classes = {},
  bg,
} = Astro.props;
---

<WidgetWrapper id={id} isDark={isDark} containerClass={`max-w-7xl mx-auto ${classes?.container ?? ''}`} bg={bg}>
  <Headline title={title} subtitle={subtitle} tagline={tagline} />
  
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-8 mt-16">
    {services.map((service) => (
      <div class="group relative">
        <!-- Service Card -->
        <div class="relative h-full p-8 bg-white dark:bg-slate-900 rounded-3xl overflow-hidden transition-all duration-500 hover:-translate-y-2 hover:shadow-2xl border border-gray-100 dark:border-slate-800 group-hover:border-blue-500/30 dark:group-hover:border-blue-400/30">
          
          <!-- Gradient Glow Effect -->
          <div class="absolute -inset-px bg-gradient-to-r from-blue-500 to-purple-500 rounded-3xl opacity-0 group-hover:opacity-10 dark:group-hover:opacity-20 blur-lg transition-opacity duration-500" aria-hidden="true"></div>
          
          <div class="relative z-10 flex flex-col h-full">
            <!-- Icon -->
            <div class="mb-6 inline-flex items-center justify-center w-14 h-14 rounded-2xl bg-blue-50 dark:bg-slate-800 text-blue-600 dark:text-blue-400 group-hover:bg-blue-600 group-hover:text-white transition-colors duration-300 shadow-sm group-hover:shadow-md group-hover:scale-110 transform">
              <Icon name={service.icon} class="w-8 h-8" />
            </div>

            <!-- Title -->
            <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-3 group-hover:text-blue-600 dark:group-hover:text-blue-400 transition-colors duration-300">
              {service.title}
            </h3>

            <!-- Description -->
            <p class="text-gray-600 dark:text-slate-400 mb-6 flex-grow leading-relaxed">
              {service.description}
            </p>

            <!-- Features (if available) -->
            {service.features && (
              <ul class="space-y-2 border-t border-gray-100 dark:border-slate-800 pt-4 mt-auto">
                {service.features.map((feature) => (
                  <li class="flex items-center text-sm text-gray-500 dark:text-slate-500">
                    <Icon name="tabler:check" class="w-4 h-4 mr-2 text-green-500" />
                    {feature}
                  </li>
                ))}
              </ul>
            )}
          </div>
        </div>
      </div>
    ))}
  </div>
  
  <!-- Bottom CTA -->
  <div class="text-center mt-16">
    <div class="inline-flex items-center space-x-3 glass px-8 py-4 rounded-full">
      <div class="w-5 h-5 bg-gradient-primary rounded-full animate-pulse"></div>
      <span class="text-lg font-medium text-gray-700 dark:text-gray-300">
        Need a custom solution? <span class="text-gradient font-semibold underline-animated cursor-pointer">Let's discuss your project</span>
      </span>
    </div>
  </div>
</WidgetWrapper> 